<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Thêm quyền truy cập bộ nhớ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Thêm quyền sử dụng camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Định nghĩa tính năng camera trước, không bắt buộc -->
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartRecyclingApp"
        tools:targetApi="31">

        <!-- Activity Manhinhchao sẽ là activity khởi động chính -->
        <activity
            android:name=".manhinhchao"
            android:exported="true"
            android:theme="@style/Theme.SmartRecyclingApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity sẽ được khởi động sau màn hình chào -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

    </application>

</manifest>

